{
  "name": "server",
  "version": "1.0.0",
  "description": "JSON server for Movie on the Tip client application",
  "main": "index.js",
  "scripts": {
    "start": "json-server json/data.json --port=4000 --delay 2000",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.0"
  }
}
